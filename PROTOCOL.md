# 维弈阁协作协议（Weiyige Collaboration Protocol）

> 版本: v1.6 | 创建: 2026-04-11 | 更新: 2026-05-21 | 状态: 生效中

协作的本质是**信息的可靠传递 + 决策的明确归属**。本协议定义了维弈阁所有 Agent 之间的协作规范。

---

> **📖 启·执事速读指引**（启只需读以下章节，其余跳过）：
> - §1.2 RACI 矩阵 — 决策权限
> - §2.2 交接块格式 — 解析成员输出
> - §3.2 阶段定义与 Agent 激活规则 — 判断链路
> - §3.4 左移检查原则 — 开发阶段门禁依据
> - §3.5 阶段门禁清单 — 审核入口
> - §6 反馈闭环 — 迭代上限

**配套文件**（本协议引用，独立维护）：
- `rules/rules-global.md` — 12 条全局规则（所有角色必须遵守）
- `rules/review-scoring.md` — 产物审核多维度评分规范
- `gates/review-reminder.md` — 审核入口强制指令
- `SKILLS-REGISTRY.md` — Skill 注册表（内置 + 外部）
- `PROJECT-CONFIG-SPEC.md` — project.yaml 字段规范

---

## 一、Agent 层级与分组

### 1.1 三层架构

```
┌─────────────────────────────────────────────┐
│              用户（唯一决策者）                 │
└──────────────────┬──────────────────────────┘
                   │
          ┌────────▼────────┐
          │   路由器/调度器    │  ← 见 ROUTER.md
          └──┬─────┬─────┬──┘
             │     │     │
        ┌────▼──┐ ┌▼──────┐ ┌▼──────────┐
        │ 战略层 │ │ 执行层 │ │  质量层     │
        │锋+砺+隐│ │枢+辞+寻│ │矩+绘+铸+鉴+盾│
        └───────┘ └───────┘ └──────────-┘
                              │
                         ┌────▼────┐
                         │ 运营层   │
                         │   算    │
                         └────────┘
```

### 1.2 决策权限矩阵（RACI）

| 决策类型 | R(负责) | A(审批) | C(咨询) | I(知会) |
|---------|---------|---------|---------|---------|
| 战略方向 | 锋 | 用户 | 隐、砺 | 枢 |
| 需求定义 | 枢 | 锋 | 砺 | 矩、绘 |
| 代码实现 | 铸 | 矩 | 枢 | 鉴 |
| UI/UX 设计 | 绘 | 枢 | 矩 | 辞 |
| 内容创作 | 辞 | 锋 | 枢 | — |
| 质量测试 | 鉴 | 枢 | 矩 | 盾 |
| 安全审计 | 盾 | 锋 | 矩 | 鉴 |
| 成本预算 | 算 | 锋 | 枢 | 全员 |
| 需求质疑 | 砺 | 用户 | 锋 | 枢 |
| 战略咨询 | 隐 | 用户 | 锋 | 砺 |

### 1.3 冲突解决优先级

当 Agent 之间产生分歧时，按以下优先级裁决：

```
用户决策 > 锋（CEO）最终裁决 > 砺（一票否决权，仅限重大风险）> 各领域负责人
```

**规则**：
1. **砺的否决权**仅在以下情况生效：数据不足、核心假设未验证、ROI 明显不合理
2. 砺否决后，锋可以 override，但必须在 STATUS.md 中记录理由
3. 技术分歧由矩裁决，设计分歧由绘裁决，质量分歧由鉴裁决
4. 跨领域分歧升级到锋

---

## 二、标准化信息传递格式

### 2.0 Team 通信命名规范

> 在 CodeBuddy Team 模式下，`task(name=...)` 和 `send_message(recipient=...)` 使用的标识符直接写入 inbox 的 `from`/`to` 字段。

**规则**：所有 team 通信标识符**必须**使用维弈阁角色简称（单字），不可使用英文职能名或拼音缩写。

| 角色 | 正确 name | 错误示例 |
|------|----------|---------|
| 锋·CEO | 锋 | ceo, feng, leader |
| 砺·合伙人 | 砺 | partner, devil, li |
| 隐·智囊 | 隐 | advisor, yin |
| 枢·PM | 枢 | pm, shu |
| 辞·内容 | 辞 | content, ci |
| 寻·探索 | 寻 | scout, xun |
| 矩·架构 | 矩 | arch, architect, ju |
| 绘·设计 | 绘 | designer, hui |
| 铸·开发 | 铸 | dev, zhu, forge |
| 鉴·QA | 鉴 | qa, jian, tester |
| 盾·安全 | 盾 | sec, dun, security |
| 算·财务 | 算 | cfo, suan, finance |
| 启·执事 | 启 | steward, qi |

**原因**：inbox 消息的 `from`/`to` 是团队协作的可追溯凭证。用维弈阁角色简称可以让消息链路与角色身份一一对应，避免 `architect`/`ju`/`designer`/`hui` 等不一致命名造成的混乱。

### 2.1 直连模式（新会话 / inline）

当用户希望单个角色独立工作、且不希望当前主会话被同步 `task` 阻塞时，推荐使用直连模式。

**触发信号**：
- “直接扮演某角色”
- “不要启动 task / 不要创建 subagent”
- “不要等待 Leader / 不进入 team 待命”
- “新开会话让某角色做”

**运行规则**：
1. 角色按自己的启动指令读取 `IDENTITY.md`、`SOUL.md`、`SKILLS.md`、`PROTOCOL.md` 和必要记忆。
2. 不调用 `task`，不创建 team，不等待 Leader 消息。
3. 直接执行用户给定任务；完成后输出交接块。
4. 若不在 CodeBuddy team 模式中，不使用 `send_message`，直接在当前会话返回结果。
5. 只有用户明确要求“启动团队 / 自动跑完 / 调度多个角色”时，才交给 `启` 进入 task/team 编排。

**推荐口令**：
```text
@寻-live
不要启动 task，不要创建 subagent，不要进入 team 待命，不要等待 Leader。
请直接以「寻·探索」身份工作。
任务：……
```

### 2.2 Agent 输出标准格式（Handoff Block）

每个 Agent 完成工作后，必须在输出末尾附加一个**交接块**，供下游 Agent 消费：

```markdown
---
## 📤 交接块（Handoff）

- **来源**: [Agent 名称]
- **阶段**: [当前项目阶段，见 STATUS.md]
- **产出类型**: [审查报告 / PRD / 设计文档 / 测试报告 / 安全报告 / 其他]
- **产物文件**: [产出文件的路径，如 `docs/reviews/2026-04-16_矩_eng-review.md`；无文件产出填 `无`]
- **状态**: [通过 / 有条件通过 / 未通过 / 需要信息]
- **关键决策**:
  1. [决策 1]: [结论]
  2. [决策 2]: [结论]
- **开放问题**:
  1. [未解决的问题]
- **下游建议**: [建议下一步由哪个 Agent 接手，做什么]
- **阻塞项**: [如果有阻塞，说明什么阻塞了谁]
---
```

### 2.3 Agent 输入标准格式（Context Block）

调用 Agent 时，应提供以下上下文：

```markdown
---
## 📥 上下文（Context）

- **项目**: [项目名称]
- **当前阶段**: [见 STATUS.md]
- **上游交接**: [来自哪个 Agent 的什么产出]
- **本次目标**: [希望这个 Agent 做什么]
- **约束**: [时间/资源/技术约束]
- **相关文件**: [列出相关文件路径]
---
```

### 2.4 决策记录格式（Decision Record）

每个重要决策必须记录：

```markdown
### DR-[编号]: [决策标题]

- **日期**: YYYY-MM-DD
- **决策者**: [Agent 名称]
- **审批者**: [Agent 名称]
- **背景**: [为什么需要这个决策]
- **方案**: [选择了什么]
- **替代方案**: [考虑过但未选择的方案]
- **理由**: [为什么选择这个方案]
- **影响**: [这个决策影响了什么]
- **可逆性**: [1-5，5=完全可逆]
```

---

## 三、项目状态追踪（STATUS.md）

每个项目根目录必须有 `STATUS.md`，所有 Agent 读写此文件来同步状态。

### 3.1 STATUS.md 模板

```markdown
# [项目名称] — 状态追踪

## 当前阶段

**阶段**: [构思 / 需求定义 / 设计 / 开发 / 测试 / 发布 / 运营]
**状态**: 🟢 On Track / 🟡 At Risk / 🔴 Blocked
**更新时间**: YYYY-MM-DD HH:MM

## 阶段历史

| 阶段 | 开始时间 | 结束时间 | 负责 Agent | 产出 | 状态 |
|------|---------|---------|-----------|------|------|
| 构思 | ... | ... | 锋+砺+隐 | 方向确认 | ✅ 完成 |
| 需求定义 | ... | ... | 枢 | PRD | 🔄 进行中 |

## 关键决策日志

[按时间倒序记录所有 DR]

## 阻塞项

| 阻塞项 | 影响谁 | 需要谁解决 | 状态 |
|--------|--------|-----------|------|
| ... | ... | ... | ... |

## 开放问题

1. [问题描述] — 待 [Agent] 回答

## NOT in scope

- [明确不做的事项]
```

### 3.2 阶段定义与 Agent 激活规则

| 阶段 | 主导 Agent | 协作 Agent | 进入条件 | 退出条件 | 门禁清单 |
|------|-----------|-----------|---------|---------|---------|
| **构思** | 锋 | 砺、隐 | 用户提出想法 | 锋确认方向 + 砺无否决 | `gates/gate-01` |
| **需求定义** | 枢 | 砺（质疑需求） | 构思阶段通过 | PRD 获锋批准 | `gates/gate-02` |
| **设计** | 绘（UI）+ 矩（架构） | 枢（产品视角） | PRD 批准 | 设计文档 + 架构方案获批 | `gates/gate-03` |
| **开发** | 铸·开发 | 矩（审查） | 设计完成 | 代码通过左移检查 + 矩审查 | `gates/gate-04` |
| **测试** | 鉴 | 盾（安全）、绘（UI验证） | 开发完成 | 测试报告通过 | `gates/gate-05` |
| **发布** | 枢（协调） | 鉴（金丝雀）、盾（安全确认） | 测试通过 | 成功上线 | `gates/gate-06` |
| **运营** | 算（成本）+ 辞（内容） | 鉴（监控） | 发布完成 | 持续 | — |

### 3.3 阶段流转规则

```
构思 ──→ 需求定义 ──→ 设计 ──→ 开发 ──→ 测试 ──→ 发布 ──→ 运营
  ↑         ↑          ↑        ↑        ↑
  └─────────┴──────────┴────────┴────────┘
              （任何阶段可回退到前序阶段）
```

**回退规则**：
1. 回退必须由当前阶段的主导 Agent 发起
2. 回退原因必须记录在 STATUS.md 的决策日志中
3. 回退到哪个阶段，该阶段的退出条件必须重新满足

### 3.4 左移检查原则（Shift-Left）

> 来源：[Fowler - Harness Engineering](https://martinfowler.com/articles/harness-engineering.html)

**核心思想**：快速的计算型检查应在**集成前**运行，不是等审查阶段。开发阶段生成代码后，**必须先通过计算型检查**再提交审查。

```
代码生成 → 左移检查（Lint + 类型 + 快速测试） → 通过 → 提交矩审查
                                                  未通过 → 修复后重跑
```

**规则**：
1. **计算型检查是开发阶段的出口条件**，不是测试阶段的入口条件
2. **必须实际执行**（`execute_command`），**禁止**不执行就声称"检查通过"
3. 不通过不进审查——不浪费推断型 Token
4. 鉴的测试阶段专注功能行为验证，计算型问题在开发阶段已解决

### 3.5 阶段门禁清单

> 审核引擎与流程检查分离设计

每个阶段有独立的门禁清单（`gates/gate-*.md`），阶段退出时**逐条勾选**。

门禁清单只含**流程完成度检查**（"该做的做了没"），产物质量评审由对应 Agent 的 Skill 评分负责。两者分离，避免清单太长 AI 跳过。

### 3.6 角色完成前自检（Self-Check）

> 角色完成工作后缺少自检环节

**规则**：每个 Agent 完成实质性工作后、输出交接块之前，**必须执行完成前自检**。自检清单在各角色 SOUL.md 末尾定义。

**统一自检项（所有角色通用）**：

- [ ] **产出完整性** — 我的核心产出文件/报告已经生成，且可通过 `read_file` 读取
- [ ] **交接块就绪** — 我已准备好结构化交接块（来源/状态/下游建议）
- [ ] **记忆回写** — 有值得记录的教训/知识/偏好已写入 `memory/`
- [ ] **状态回写** — 我已更新以下状态文件（**必须，不可跳过**）：
  - `ai-workspace/{task_id}/state.json` — 更新 `phase`（当前阶段：ideation/requirement/design/development/testing/summary）、`active_agent`（我的角色名）、`phase_progress`（如 "3/6"）、`updated_at`、`phases_completed`
  - `ai-workspace/project-status.json` — 更新 `active_task`、`active_phase`、`active_agent`、`phase_progress`、`updated_at`、`requires_human_decision`（如需用户决策则填入）；任务完成时设 `active_task` 为 null 并填 `last_completed_task` 和 `last_success_at`
- [ ] **不越界** — 我没有做不属于我职责的事（参考 SOUL.md "不做"清单）
- [ ] **无遗留** — 没有发现了但未处理的问题（要么修复、要么升级到对应 Agent、要么记录到 NOT in scope）

**角色专属自检项**：见各角色 SOUL.md 中的「完成前自检」章节。

---

## 四、协作流程标准

### 4.1 标准协作链路

```
用户输入
   │
   ▼
┌──────────┐  方向确认   ┌──────────┐  质疑挑战   ┌──────────┐
│  CEO_锋   │ ──────────▶│ 顾问_隐   │ ──────────▶│ 合伙人_砺 │
│  方向决策  │            │ 多元审视   │            │ 魔鬼代言  │
└────┬─────┘            └──────────┘            └────┬─────┘
     │                                                │
     │◀──────────── 通过/否决 + 交接块 ────────────────┘
     │
     ▼ 方向确定（STATUS: 构思→需求定义）
┌──────────┐  技术评估   ┌──────────┐
│  PM_枢    │ ──────────▶│ 架构_矩   │
│  需求拆解  │            │ 架构设计   │
└────┬─────┘            └────┬─────┘
     │                       │
     ▼ PRD 批准（STATUS: 需求定义→设计）
┌──────────┐            ┌──────────┐
│ 设计_绘   │            │ 开发_铸   │
│ UX 设计   │            │ 代码实现   │
└────┬─────┘            └────┬─────┘
     │                       │
     ▼ 设计+开发完成（STATUS: 开发→测试）
┌──────────┐  安全审计   ┌──────────┐
│  QA_鉴    │ ──────────▶│ 安全_盾   │
│  质量测试  │            │ 威胁建模   │
└────┬─────┘            └──────────┘
     │
     ▼ 测试通过（STATUS: 测试→发布）
┌──────────┐            ┌──────────┐
│ 内容_辞   │            │ 财务_算   │
│ 内容运营   │            │ 成本追踪   │
└──────────┘            └──────────┘
```

### 4.2 跨 Agent 调用协议

**直接调用**（Agent A 可以在输出中建议调用 Agent B）：

| 调用方 | 可直接建议调用 | 场景 |
|--------|-------------|------|
| 锋 | 枢、隐、砺 | 方向确定后交枢拆解；需要深度分析找隐；需要质疑找砺 |
| 枢 | 矩、绘、铸、鉴、算 | 需求确定后交矩评估架构；交绘设计；交铸开发；交鉴测试；交算估成本 |
| 矩 | 鉴、盾、铸 | 架构审查发现测试缺口交鉴；发现安全问题交盾；审查通过交铸实现 |
| 铸 | 鉴 | 代码实现完成后交鉴测试；Bug 修复后交鉴回归验证 |
| 绘 | 鉴、铸 | 设计完成后交鉴验证 UI；交铸按设计稿实现 |
| 鉴 | 盾、铸、绘 | 发现安全问题交盾；发现 Bug 交铸修复；发现 UI 问题交绘 |
| 盾 | 铸、鉴 | 安全修复建议交铸实现；安全确认后交鉴验证 |
| 砺 | 锋 | 否决后交锋最终裁决 |
| 隐 | 锋、砺 | 分析完成后交锋决策；建议砺质疑；Deep 设计文档二评 |
| 辞 | 锋 | 内容完成后交锋审批 |
| 算 | 锋、枢 | 成本预警交锋决策；预算数据交枢规划 |

### 4.3 Office Hours v2 专用交接

> 详细规则见 `合伙人_砺/SOUL.md` 的 Office Hours 章节。

核心要求：
1. **Related Design Discovery** — 砺生成方案前必须搜索历史设计文档，发现相似设计时标注 Supersedes/Related
2. **Cross-Agent Second Opinion** — Deep 模式设计文档完成后交隐做独立二评
3. **Visual Sketch Handoff** — 涉及 UI/页面体验时交绘产出低保真草图
4. **Spec Review Loop** — Deep 模式设计文档最多 3 轮审查（完整性/一致性/清晰度/范围/可行性）

### 4.4 信息不丢失原则

1. **交接块必填**：每个 Agent 输出必须包含交接块
2. **STATUS.md 必更新**：阶段变更时必须更新 STATUS.md
3. **决策必记录**：所有重要决策写入 STATUS.md 的决策日志
4. **开放问题必追踪**：未解决的问题记录在 STATUS.md，不允许"静默丢失"

---

## 五、文件系统约定

### 5.1 项目目录结构

```
project-root/
├── STATUS.md              # 项目状态追踪（所有 Agent 共享）
├── TODOS.md               # 延期事项追踪
├── docs/
│   ├── designs/           # 设计文档（砺的 office-hours 产出）
│   ├── prd/               # PRD 文档（枢的产出）
│   ├── reviews/           # 审查报告（锋、矩、绘、鉴、盾的产出）
│   └── decisions/         # 决策记录归档
├── memory/                # 项目级记忆（见 MEMORY.md）
└── src/                   # 源代码
```

### 5.2 文件命名规范

- 审查报告：`reviews/YYYY-MM-DD_[agent]_[类型].md`（如 `2026-04-11_矩_eng-review.md`）
- PRD：`prd/[项目名]_PRD_v[版本].md`
- 设计文档：`designs/[项目名]_design.md`
- 决策记录：`decisions/DR-[编号]_[标题].md`

### 5.3 产物验证规则

**核心原则**：产物必须 `read_file` 实际可读，不可声称已存在（见 rules-global W3、W12）。

**必须落盘的产物**：

| 阶段 | 产物 | 路径模式 | 负责 |
|------|------|---------|------|
| 需求 | PRD | `docs/prd/[项目名]_PRD_v[N].md` | 枢 |
| 设计 | 架构审查 | `docs/reviews/YYYY-MM-DD_矩_eng-review.md` | 矩 |
| 设计 | 设计审查 | `docs/reviews/YYYY-MM-DD_绘_design-review.md` | 绘 |
| 测试 | QA 报告 | `docs/reviews/YYYY-MM-DD_鉴_qa-report.md` | 鉴 |
| 测试 | 安全审计 | `docs/reviews/YYYY-MM-DD_盾_security-audit.md` | 盾 |

启在 auto 模式下 spawn 下一个 Agent 前验证上游产物可读，不可读则暂停。文件系统异常时降级为交接块内嵌（标注「降级」），门禁标记 ⚠️。

---

## 六、反馈闭环机制

### 6.1 四种审查结果

| 结果 | 后续动作 | 是否重审 |
|------|---------|---------|
| ✅ 通过 | 进入下一阶段 | 否 |
| ⚠️ 有条件通过 | 修改后审查方快速确认 | 仅确认 |
| ❌ 未通过 | 修改后完整重审 | 是 |
| ❓ 需要信息 | 补充后继续 | 继续当前 |

### 6.2 迭代上限

| 审查类型 | 最大轮次 | 超限处理 |
|---------|---------|---------|
| CEO 计划审查（锋） | 3 | 升级到用户 |
| 工程审查（矩） | 3 | 记录未解决，继续 |
| 设计审查（绘） | 3 | 记录设计债，继续 |
| QA 测试（鉴） | 2 | 标记已知问题 |
| 安全审计（盾） | 2 | 标记已知风险 |
| 需求质疑（砺） | 2 | 锋最终裁决 |

超限后：记录到 STATUS.md "开放问题" + TODOS.md 追踪。

### 6.3 审查回应格式

提交方修改后必须附带回应块：

```markdown
## 🔄 审查回应
- **回应方**: [Agent]
- **针对**: [审查方 + 日期]
- **轮次**: 第 N 轮

| # | 审查意见 | 处理 | 说明 |
|---|---------|------|------|
| 1 | [意见] | ✅已修/⏭️延期/❌不同意 | [说明] |
```

未采纳项必须给理由，延期项写入 TODOS.md。

---

## 七、紧急通道

### 7.1 升级机制

当任何 Agent 遇到以下情况时，必须立即升级到锋：

| 触发条件 | 升级到 | 响应要求 |
|---------|--------|---------|
| 安全漏洞（CRITICAL） | 锋 + 盾 | 立即处理 |
| 预算超支（> 95%） | 锋 + 算 | 24 小时内决策 |
| 核心假设被推翻 | 锋 + 砺 | 重新评估方向 |
| 技术方案不可行 | 锋 + 矩 | 重新评估范围 |
| 阶段阻塞超过 48 小时 | 锋 | 介入协调 |

### 7.2 快速通道

对于小型任务（预计 < 2 小时），可以跳过完整流程：

```
用户 → 直接调用目标 Agent（Quick 模式）→ 产出 → 完成
```

快速通道条件：
- 不涉及战略方向变更
- 不涉及新的架构决策
- 不涉及安全敏感操作
- 用户明确指定 Quick 模式

---

## 八、协议维护

### 8.1 版本管理

本协议随项目演进而更新。每次修改需要：
1. 更新版本号
2. 在变更日志中记录修改内容
3. 通知所有 Agent（通过 STATUS.md）

### 8.2 变更日志

| 版本 | 日期 | 变更内容 |
|------|------|---------|
| v1.5 | 2026-05-01 | 新增 2.1 直连模式：单角色可在新会话/当前会话直接扮演运行，不启动 task、不等待 Leader，用于避免主会话被同步任务阻塞 |
| v1.4 | 2026-04-17 | 新增 2.0 Team 通信命名规范：team 模式下 name/recipient 必须使用维弈阁角色简称（锋/砺/隐/枢/辞/寻/矩/绘/铸/鉴/盾/算/启），禁止英文职能名（architect/designer/pm 等），确保 inbox 消息 from/to 与角色身份一致 |
| v1.3 | 2026-04-16 | 新增铸·开发角色：开发阶段由铸负责代码实现；RACI 矩阵新增"代码实现"行；跨 Agent 调用协议更新（矩→铸、铸→鉴）；寻·探索 agent 文件补齐 |
| v1.2 | 2026-04-16 | 增加产物验证机制：交接块增加 `产物文件` 字段；新增 5.3 产物验证规则；6 个 gate 门禁增加文件存在性检查项；启·执事增加 spawn 前产物文件验证步骤 |
| v1.1 | 2026-04-11 | 增加第六章：反馈闭环机制（闭环模型、迭代次数限制、审查回应格式、各环节闭环规则） |
| v1.0 | 2026-04-11 | 初始版本：定义协作协议、RACI 矩阵、信息传递格式、项目状态追踪 |

---

*本协议是维弈阁团队协作的基础设施。没有协议，13 个 Agent 就是 13 个孤岛。*
