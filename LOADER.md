# 维弈阁加载协议（Weiyige Loader）

> 版本: v2.0 | 创建: 2026-05-13 | 更新: 2026-05-21 | 状态: 生效中

分级加载策略——减少首次激活 token 消耗，按需深入。

---

## 加载层级

| 层级 | 名称 | 文件 | 触发条件 | token 量 |
|------|------|------|---------|---------|
| **L0** | 身份卡片 | `IDENTITY.md` | 角色激活时**自动加载** | < 500 |
| **L0-shared** | 共享规则 | `SHARED.md` | 首次角色激活时**自动加载一次** | ~800 |
| **L1** | 方法论 | `SOUL.md` | 需要深度执行时**按需加载** | 1000-3000 |
| **L2** | 领域知识 | `memory/*.md` + `skills/` | 特定技能/项目上下文时加载 | 按需 |

---

## 加载规则

### L0 自动加载（必须）

角色被路由激活时，**读取两个文件**：

1. `SHARED.md` — 公共规则（仅首次角色激活时加载一次，多角色串行时不重复）
2. `{角色}/IDENTITY.md` — 角色专属身份卡片

### L1 按需加载（触发词）

| 触发条件 | 说明 |
|---------|------|
| 用户说 `@soul` / `/load detail` / `深度模式` | 显式触发 |
| 路由模式为 **Deep** | 自动升级 |
| 任务被 **启·执事** 编排调度 | team 模式下自动读 SOUL |
| 角色需要执行**核心职责**（非简单问答） | 智能判断 |

**判断标准**：如果 IDENTITY.md 中的信息足够完成任务，不加载 SOUL.md。

### L2 领域知识（特定场景）

| 触发条件 | 加载内容 |
|---------|---------|
| 角色有项目特定记忆 | `memory/knowledge.md` |
| 角色有历史教训 | `memory/lessons.md` |
| 任务涉及特定技能 | `skills/{skill}/SKILL.md` |

---

## 启·执事的加载策略（v2.0 优化）

启的加载策略与其他角色不同——启作为 Leader 需要更多上下文，但也需要控制总量。

### 启的必读文件（初始化时）

| 文件 | 大小 | 说明 |
|------|------|------|
| `执事_启/IDENTITY.md` | ~0.8KB | 身份 + 启动指针 |
| `执事_启/SOUL.md` | **~8KB** | 核心调度逻辑（已精简） |

**总计：~9KB（约 2500 token）**

### 启的按需读取文件

| 文件 | 何时读取 |
|------|---------|
| `执事_启/ai-workspace-protocol.md` | 初始化工作区时 |
| `gates/two-layer-gate.md` | 执行门禁审核时 |
| `gates/gate-{N}.md` | 对应阶段审核时 |
| `执事_启/evolution-protocol.md` | 全链路完成后 |
| `执事_启/exception-matrix.md` | 遇到异常时 |
| `PROTOCOL.md`（速读章节） | 需要查 RACI/交接块格式时 |

### 启不需要读取的文件

| 文件 | 原因 |
|------|------|
| `ROUTER.md` | 路由是主 Agent 的事，启已知链路 |
| `LOADER.md` | 加载协议是主 Agent 的事 |
| `执事_启/start.md` | 启动协议是主 Agent 的事 |
| 其他角色的 `SOUL.md` | 只在 inline 模式切换角色时按需读取 |

---

## 与 ROUTER.md 的关系

```text
用户输入 → ROUTER.md 路由到角色（主 Agent 执行）
                │
                ▼
         LOADER.md 决定加载深度（主 Agent 执行）
                │
          ┌─────┼─────┐
          L0    L1    L2
       (身份) (方法) (知识)
```

---

## 优化前后对比

| 指标 | 优化前（v1.0） | 优化后（v2.0） |
|------|---------------|---------------|
| 启初始化 token | ~18,000-20,000（70KB） | ~2,500（9KB） |
| 启必读文件数 | 8 个 | 2 个 |
| 按需文件 | 0（全量加载） | 6 个（需要时才读） |
| 其他角色 Quick 模式 | ~2000-5000 | ~100-200（L0 only） |
| token 节省率 | — | 启: 85%+, 其他: 50-90% |

---

## 变更日志

| 版本 | 日期 | 变更 |
|------|------|------|
| v2.0 | 2026-05-21 | 新增启·执事专属加载策略；SOUL.md 精简后更新 token 估算；明确启不需要读取的文件 |
| v1.0 | 2026-05-13 | 初始版本：分级加载策略 |

---

*加载协议的目标：只在需要时加载需要的深度。*
