# 维弈阁加载协议（Weiyige Loader）

> 版本: v1.0 | 创建: 2026-05-13 | 状态: 生效中

分级加载策略——减少首次激活 token 消耗，按需深入。

---

## 加载层级

| 层级 | 名称 | 文件 | 触发条件 | token 量 |
|------|------|------|---------|---------|
| **L0** | 身份卡片 | `IDENTITY.md` | 角色激活时**自动加载** | < 500 |
| **L0-shared** | 共享规则 | `SHARED.md` | 首次角色激活时**自动加载一次** | ~800 |
| **L1** | 方法论 | `SOUL.md` | 需要深度执行时**按需加载** | 1000-5000 |
| **L2** | 领域知识 | `memory/*.md` + `skills/` | 特定技能/项目上下文时加载 | 按需 |

---

## 加载规则

### L0 自动加载（必须）

角色被路由激活时，**读取两个文件**：

1. `SHARED.md` — 公共规则（三档模式、自检清单、CLI 规范、Git 规范等），**仅首次角色激活时加载一次**，多角色串行时不重复加载
2. `IDENTITY.md` — 角色专属身份卡片

```
读取: .weiyige/SHARED.md        （首次，后续角色切换不重复）
读取: .weiyige/{角色}/IDENTITY.md
```

### L1 按需加载（触发词）

以下情况自动升级到 L1：

| 触发条件 | 说明 |
|---------|------|
| 用户说 `@soul` / `/load detail` / `深度模式` | 显式触发 |
| 路由模式为 **Deep** | 自动升级 |
| 任务被 **启·执事** 编排调度 | team 模式下自动读 SOUL |
| 角色需要执行**核心职责**（非简单问答） | 智能判断 |

```
读取: .weiyige/{角色}/SOUL.md
```

**判断标准**：如果 IDENTITY.md 中的信息足够完成任务，不加载 SOUL.md。

### L2 领域知识（特定场景）

| 触发条件 | 加载内容 |
|---------|---------|
| 角色有项目特定记忆 | `memory/knowledge.md` |
| 角色有历史教训 | `memory/lessons.md` |
| 任务涉及特定技能 | `skills/{skill}/SKILL.md` |

```
读取: .weiyige/{角色}/memory/*.md
读取: .weiyige/skills/{skill}/SKILL.md
```

---

## 与 ROUTER.md 的关系

```text
用户输入 → ROUTER.md 路由到角色
                │
                ▼
         LOADER.md 决定加载深度
                │
          ┌─────┼─────┐
          L0    L1    L2
       (身份) (方法) (知识)
```

- **Quick 模式** → L0 only
- **Standard 模式** → L0，需要时升级 L1
- **Deep 模式** → L0 + L1
- **Team 模式** → L0 + L1（启编排时自动加载 SOUL）

---

## 预期收益

| 指标 | 优化前 | 优化后 |
|------|--------|--------|
| 角色激活 token | ~2000-5000 | ~100-200（L0 only） |
| 简单问答场景 | 全量加载 | 仅 L0 |
| 深度执行场景 | 全量加载 | L0 + L1（按需） |
| token 节省率 | — | 50-90%（Quick 场景） |

---

*加载协议的目标：只在需要时加载需要的深度。*
